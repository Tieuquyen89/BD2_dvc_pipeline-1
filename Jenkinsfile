pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                
                sh "build is called"
            }
        }
        stage('Test') { 
            steps {
                // 
                sh "test is called"
            }
        }
        stage('Deploy') { 
            steps {
                // 
                sh "deploy is called"
            }
        }
    } 
}