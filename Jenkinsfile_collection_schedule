pipeline {
    agent any 
    stages {
        stage('Data acquisition') { 
            steps {
                
                sh ('chmod +x ./twitter_collection.py')
            }
        }
        
        } 
}
