pipeline {
    agent any 
    stages {
        stage('Data acquisition') { 
            steps {
                
                echo ('chmod +x ./twitter_collection.py')
            }
        }
        
        } 
}
